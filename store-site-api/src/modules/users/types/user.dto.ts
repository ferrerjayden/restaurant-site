export default interface CreateUserDTO {
    userName: string;
    email: string;
    password: string;
}

export default interface UpdateUserDTO {
    userName: string;
    email: string;
    password: string;
}